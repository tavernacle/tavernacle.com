# Use legacy peer deps to handle peer dependency conflicts
# This helps when dependencies haven't caught up with latest React versions
legacy-peer-deps=true

# Use exact versions from package-lock for reproducible builds
package-lock=true

# Strict engine check
engine-strict=true
